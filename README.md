# Bielik – Codespaces (CPU)

## Start lokalny w Codespaces
1. Open in Codespaces → wybierz maszynę **4-core / 8 GB**.
2. Terminal: aktywuj venv `source .venv/bin/activate` (jeśli nie aktywne).
3. Uruchom API:

